# Copy to .env.local for local overrides, or set in Vercel dashboard for prod.

# Points the built frontend at the deployed backend origin.
# Leave blank in dev (Vite proxies /api → :8000).
VITE_API_URL=https://aeronova-api.onrender.com
